---
# yaml-language-server: $schema=https://schema.blue-build.org/module-v1.json
# Optional: Install Lix (community Nix implementation) on an ostree/atomic image.
# Not included in the main recipe by default. To use, add:
#   - from-file: module-recipes/lix.yml
# to your recipe's modules (remove the existing nix.yml line to avoid duplicate installs).
modules:
  - type: script
    scripts:
      - install-lix.sh
