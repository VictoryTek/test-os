---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:
  - type: files
    files:
      - source: modules/firstboot/files
        destination: /
      - source: files/firstboot/
        destination: /usr/lib/firstboot-scripts/tasks/
