---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:
  - type: firstboot
    source: local
    scripts:
      - name: 00-test.sh
        mode: system
      # Removed 01-install-nix.sh, no longer used
        
  - type: systemd
    system:
      enabled:
        - firstboot.service
    user:
      enabled:
        - firstboot-login.service