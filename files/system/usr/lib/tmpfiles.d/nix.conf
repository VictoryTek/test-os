# Setup /var/nix directory for Nix package manager on immutable OS
# A bind mount (nix.mount) maps /var/nix to /nix at boot
# Only create the base directory - let the Nix installer create the rest

# Create main /var/nix directory
d /var/nix 0755 root root - -
