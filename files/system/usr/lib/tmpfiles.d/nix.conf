# Setup /nix as a symlink to /var/nix for Nix package manager on immutable OS
# This is necessary because the root filesystem is read-only on OSTree systems

# Create /var/nix directory
d /var/nix 0755 root root - -

# Create /nix as a symlink to /var/nix
L+ /nix - - - - /var/nix
